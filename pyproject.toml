[project]
name = "agent-security-gateway"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "anthropic>=1.8.0",
    "fastapi[standard]>=0.141.1",
    "pydantic>=2.13.5",
    "python-dotenv>=1.2.3",
]
